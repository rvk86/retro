import React, { PropTypes, Component } from 'react';

import './**NAME**.css';

class **NAME** extends Component {
  static propTypes = {
    // prop: PropTypes.func
  }

  render() {
    return (
      <div className="**NAME**">
      </div>
    );
  }
}

export default **NAME**;
